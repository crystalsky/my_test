#ifndef std_with_lambda_h__
#define std_with_lambda_h__

#include "Base.h"

class std_with_lambda : public my_kit::base
{
	DECLARE_REFLECTED_CLASS(std_with_lambda)
public:
	std_with_lambda();
	~std_with_lambda();

public:
	virtual void run();
protected:
	
private:

};

#endif // std_with_lambda_h__
